# 서버
HOST=0.0.0.0
PORT=8000
PUBLIC_BASE_URL=https://teamplay-talk.tech

# (P1+) DB — DO Managed Postgres 또는 Supabase
DATABASE_URL=

# (P1+) 카카오 OAuth
KAKAO_REST_API_KEY=
KAKAO_CLIENT_SECRET=
KAKAO_REDIRECT_URI=https://teamplay-talk.tech/auth/kakao/callback

# 초대 링크 클릭만으로 카카오 OAuth + 방 참여까지 끝내는 실험 기능.
# 프로토타입/대회 제출 기본값은 false로 두고, PlayMCP 초대 코드 참여를 사용한다.
# 켤 때는 INVITE_STATE_SECRET 또는 KAKAO_CLIENT_SECRET이 반드시 필요하다.
INVITE_OAUTH_ENABLED=false
INVITE_STATE_SECRET=

# 데일리 운영 자동화 (KST)
# 하이브리드 배포 시 한 서버에서만 true로 설정해 중복 발송을 막는다.
SCHEDULER_ENABLED=true

# 밤 체크인 폼: 전날/오늘/예정 todo 완료 여부 수집
DAILY_CHECKIN_ENABLED=true
DAILY_CHECKIN_HOUR_KST=21

# 아침 팀 리포트: 전날 체크인 반영 + 팀 상태 공지
DAILY_REPORT_ENABLED=true
DAILY_REPORT_HOUR_KST=9

# 개인별 오늘 할일 digest
DAILY_TASK_DIGEST_ENABLED=true
DAILY_TASK_DIGEST_HOUR_KST=9

# 저장 토큰 암호화 키 (Fernet). DB에 들어가는 카카오 access/refresh 토큰을
# 앱 계층에서 암호화한다. `uv run python scripts/gen_token_key.py`로 생성해 붙여넣기.
# 미설정이면 평문 저장(개발 편의)이며, 운영 배포에는 반드시 설정할 것.
TOKEN_ENC_KEY=

# Google Drive 연동의 OAuth는 호스트(PlayMCP)가 대행하므로 서버 .env에
# Google 자격증명이 필요 없다. Client ID/Secret/Endpoint/Scope는 PlayMCP의
# MCP 등록 폼(인증 방식: OAuth)에 입력한다.
